# Анализ проекта MapsNews

## Общее описание
**MapsNews** — это веб-приложение для агрегации новостей с географической привязкой. Система автоматически собирает новости с портала news29.ru, анализирует их текст для выявления адресов, определяет географические координаты и отображает события на интерактивной карте. 

## 🏗 Архитектура

Проект реализован как микросервисная система, объединяющая бэкенд и фронтенд, которые могут быть развернуты через Docker Compose.

### 1. Backend (Серверная часть)
*   **Путь**: `/backend`
*   **Язык**: Python
*   **Фреймворк**: FastAPI
*   **База данных**: SQLite (`news.db`)

**Ключевые модули:**
*   **API**: Предоставляет REST-эндпоинты для фронтенда и бота.
*   **Parser (`auto_parser`)**: Фоновый процесс для периодического сбора новостей из RSS-ленты.
*   **Advanced Geocoder**: Модуль геокодирования, использующий:
    *   **Natasha**: NLP-библиотека для извлечения именованных сущностей (адресов) из русского текста.
    *   **Yandex Maps API**: Для преобразования строковых адресов в координаты (широта/долгота).
* 

### 2. Frontend (Клиентская часть)
*   **Путь**: `/frontend`
*   **Язык**: JavaScript
*   **Фреймворк**: React
*   **Сборка**: Create React App (`react-scripts`)

**Основные компоненты:**
*   **Карта**: Интеграция через `@pbe/react-yandex-maps` для отображения меток новостей.
*   **Лента новостей**: Сайдбар со списком последних событий.
*   **Детальный просмотр**: Страница новости с полным текстом (рендеринг через `marked` и `dompurify`).

## 🛠 Технологический стек

### Backend
*   **Core**: `FastAPI`, `Uvicorn`
*   **Data Scrapping**: `BeautifulSoup4`, `feedparser`, `requests`
*   **NLP & Geo**: `Natasha` (NER), Yandex Maps API
*   **Database**: `SQLite` (через `sqlite3`)
*   **Bot**: `aiogram`

### Frontend
*   **Core**: `React`, `ReactDOM`
*   **Routing**: `react-router-dom`
*   **HTTP Client**: `axios`
*   **Maps**: `@pbe/react-yandex-maps`
*   **Utilities**: `dompurify` (безопасность), `marked` (Markdown)

## 🚀 Функциональный процесс (Data Flow)

1.  **Сбор данных**: Скрипт в `main.py` запускает мониторинг RSS-ленты.
2.  **Обработка**: Полученные новости очищаются от лишних тегов.
3.  **Геокодирование**:
    *   Текст новости проходит через NLP-модель Natasha.
    *   Выделенные адреса отправляются в Яндекс.Карты.
    *   Полученные координаты привязываются к новости.
4.  **Визуализация**: Фронтенд запрашивает данные по API и размещает метки на карте. Пользователь может видеть "тепловую карту" событий.
